const AmountBalance = ({debit, credit, balance}) => {
   
    return(
        <div>
            <p className="Amount"> Amount Balance: ${balance}  </p>
        </div>
    );
}

export default AmountBalance;